{"name":"Deploy","on":{"push":{"branches":["main"]},"workflow_dispatch":{}},"permissions":{"contents":"write","pages":"write","id-token":"write"},"concurrency":{"group":"pages","cancel-in-progress":true},"jobs":{"deploy":{"runs-on":"ubuntu-latest","environment":{"name":"github-pages","url":"${{ steps.deployment.outputs.page_url }}"},"steps":[{"uses":"actions/checkout@v4"},{"name":"Import source from zip","run":"if [ -f marea-nextjs-source.zip ]; then unzip -q -o marea-nextjs-source.zip && shopt -s dotglob && rm -rf site/.github && cp -rf site/* . && rm -rf site marea-nextjs-source.zip && git config user.name marea-bot && git config user.email bot@users.noreply.github.com && git add -A -- ':!.github' && git commit -m 'import project source [skip ci]' && git push; fi"},{"uses":"actions/setup-node@v4","with":{"node-version":22}},{"run":"npm ci"},{"name":"Mirror assets","run":"node scripts/download-assets.mjs && git config user.name marea-bot && git config user.email bot@users.noreply.github.com && git add public/assets && (git diff --cached --quiet || (git commit -m 'mirror assets [skip ci]' && git push))"},{"name":"Build","env":{"BASE_PATH":"/marea-mx","NEXT_PUBLIC_ASSETS_BASE":"/marea-mx/assets/"},"run":"npm run build && touch out/.nojekyll"},{"uses":"actions/configure-pages@v5","with":{"enablement":true}},{"uses":"actions/upload-pages-artifact@v3","with":{"path":"out"}},{"id":"deployment","uses":"actions/deploy-pages@v4"}]}}}
